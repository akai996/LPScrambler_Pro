name: Build LP_Obfuscator EXE 卫士版

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build_windows_exe:
    runs-on: windows-latest

    steps:
    # 1. 拉取代码
    - name: Checkout Code
      uses: actions/checkout@v4

    # 2. 设置 Python
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    # 3. 安装依赖 (包含 beautifulsoup4)
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install beautifulsoup4 pyinstaller

    # 4. 调试：确认文件是否存在 (已修复为 PowerShell 语法)
    - name: 🔍 Debug - Verify File Exists
      run: |
        echo "正在列出当前目录文件..."
        ls
        if (Test-Path "LP_Scrambler_V5_Guard.py") {
            echo "✅ 成功找到 LP_Scrambler_V5_Guard.py"
        } else {
            echo "❌ 严重错误：在根目录下找不到 LP_Scrambler_V5_Guard.py"
            exit 1
        }

    # 5. 打包 EXE
    - name: Build with PyInstaller
      run: pyinstaller --onefile --noconsole --name "LP_Scrambler_Guard" LP_Scrambler_V5_Guard.py

    # 6. 上传产物
    - name: Upload EXE Artifact
      uses: actions/upload-artifact@v4
      with:
        name: LP_Scrambler_Guard_EXE
        path: dist/LP_Scrambler_Guard.exe
